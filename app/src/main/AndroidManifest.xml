<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.lijj.wyx.physical">

    <application
        android:name="com.lijj.wyx.physical.app.WanAndroidApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.main.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.lijj.wyx.physical.MainActivity"
            android:exported="false"
            android:screenOrientation="portrait"></activity>

        <activity android:name=".ui.main.LoginActivity"></activity>
        <activity android:name=".ui.hierarchy.KnowledgeHierarchyDetailActivity"></activity>
        <activity android:name=".ui.main.SearchListActivity"></activity>

        <activity android:name=".ui.main.RegisterActivity"></activity>

        <activity android:name=".ui.main.ArticleDetailActivity"></activity>
        <meta-data
            android:name="design_width_in_dp"
            android:value="360" />
        <meta-data
            android:name="design_height_in_dp"
            android:value="640" />
    </application>

</manifest>
